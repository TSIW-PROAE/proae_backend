export enum EnumTipoInput {
  INPUT = 'input',
  SELECT = 'select',
  MULTI_SELECT = 'multi-select',
  SINGLE_SELECT = 'single-select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  TEXTAREA = 'textarea',
  FILE = 'file',
  DATE = 'date',
  TIME = 'time',
  DATETIME = 'datetime-local',
  RANGE = 'range'
}