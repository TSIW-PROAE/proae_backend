export enum EnumTipoInput {
  TEXT = 'text',
  NUMBER = 'number',
  PASSWORD = 'password',
  EMAIL = 'email',
  TEXT_AREA = 'textarea',
  SELECT = 'select',
  RADIO = 'radio',
  DATE = 'date',
  FILE = 'file',
  SELECT_GROUP = 'selectGroup',
  TEXT_INPUT_GROUP = 'textInputGroup',
}
